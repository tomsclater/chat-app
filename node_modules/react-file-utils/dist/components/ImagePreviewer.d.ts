import { MouseEvent } from 'react';
import type { ImageUpload } from '../types';
declare type CustomMouseEvent = (id: string, event: MouseEvent<HTMLButtonElement>) => void;
export declare type ImagePreviewerProps = {
    /** The list of image uploads that should be displayed */
    imageUploads?: ImageUpload[];
    /** A callback to call when the remove icon is clicked */
    handleRemove?: CustomMouseEvent;
    /** A callback to call when the retry icon is clicked */
    handleRetry?: CustomMouseEvent;
    /** A callback to call with newly selected files. If this is not provided no
     * `ThumbnailPlaceholder` will be displayed.
     */
    handleFiles?: (files: File[]) => void;
    /** Allow drag 'n' drop (or selection from the file dialog) of multiple files */
    multiple?: boolean;
    disabled?: boolean;
};
export declare const ImagePreviewer: ({ multiple, disabled, imageUploads, handleRemove, handleRetry, handleFiles, }: ImagePreviewerProps) => JSX.Element;
export {};
//# sourceMappingURL=ImagePreviewer.d.ts.map